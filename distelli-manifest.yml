pedro/nodejs-teste:

  Build:
    - npm install
    - npm test

  PkgInclude:
    - '*'

  PkgExclude:
    - node_modules/

  PreInstall:
    - sudo apt-get -y update
    - sudo apt-get -y install nodejs

  Env:
    - PORT: "3000"

  Exec:
    - nodejs app.js